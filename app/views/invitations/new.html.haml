%section.invite.container
  .row
    .span10.offset1
      = form_for @invitation, method: :post, class: "invite" do |f|
        %header
          %h1 Invite a friend to join MyFlix!
        %fieldset
          = f.label :recipient_name, "Friend's Name"
          = f.text_field :recipient_name
          = f.label :recipient_email, "Friend's Email Address"
          = f.text_field :recipient_email
          = f.label :message, "Invitation Message"
          = f.text_area :message, placeholder: "Please join this really cool site!", class: "span4", rows: 6
        %fieldset.action
          = f.submit "Send Invitation", class: "btn"
